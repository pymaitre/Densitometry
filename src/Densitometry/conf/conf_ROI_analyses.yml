    directory_out : 'F:\Belardo\BAROTRAUMA\Analyses\OSR\TS\Covid\Problems'

    
    specific_ROI_analyses : True #  'False'
    
    # insert specific ROI delimiter: HU_min, HU_max, min_counts.
    specific_ROI_delimiter : [-200, 200, 0]
    # specific_ROI_delimiter : [-778, -34, 0]

    over_ROI_analyses : False #  ' True'
    
    #insert over ROI delimiter: HU_min over whose analyse; min_counts for this region.
    over_ROI_delimiter : [400, 100]
    
    rate_ROI_analyses : False #  ' True'

    #insert rate ROI delimiter: HU_min over whose analyse; rate_counts beetween inside and outside for this region.
    rate_ROI_delimiter : [400, 1]