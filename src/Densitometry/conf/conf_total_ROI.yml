    directory_dcm_out : "C:\\Users\\volon\\Desktop\\check"

    directory_out : "C:\\Users\\volon\\Desktop\\output_1"

    image_modality : "CT"

    save_spacing_histo :  True  #False #

    save_ROI_info : True #False #
    
    total_ROI_analyses :  True #False  #

    show_total_ROI_info : False  # True #

    save_total_ROI_info :  True #False  #

    flag_resampling: True #Flag for the resampling (True: resample, False: dot not resample)

    flag_new_spacing: "min_global" #Flag to choose the spacing (min_global, mean_global, max_global,frequency,manual)

    new_spacing: [0.78125, 0.78125,2] #Insert the desired new spacing (during the resampling)

    flag_parallel: False #Flag to manage parallelization (if True: insert manually N_jobs below, if False: N_jobs=1 automatically)

    N_jobs: 3 

    resampler: sitkBSpline #choose the proper resampler

    ID_problems : []

    rt_kind : 'MV_RQ'

    list_roi : ['Heart']
   