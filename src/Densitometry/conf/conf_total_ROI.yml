    directory_dcm_out : "C:\\Users\\volon\\Desktop\\test" #input directory

    directory_out : "C:\\Users\\volon\\Desktop\\test_out" #output directory

    image_modality : "CT"

    save_spacing_histo :  True  #False #

    save_ROI_info : True #False #
    
    total_ROI_analyses :  True #False  #

    show_total_ROI_info : False  # True #

    save_total_ROI_info :  True #False  #

    flag_resampling: True #Flag for the resampling (True: resample, False: dot not resample)

    flag_new_spacing: "manual" #Flag to choose the spacing (min_global, mean_global, max_global,frequency, manual)

    new_spacing: [1.074743845, 1.074743845,4.391967871] #Insert the desired new spacing (during the resampling)

    flag_parallel: False #Flag to manage parallelization (if True: insert manually N_jobs below, if False: N_jobs=1 automatically)

    N_jobs: 3 #Number of jobs used during parallelization

    resampler: sitkBSpline #choose the proper resampler (e.g. sitkBSpline, sitkNearestNeighbor, sitkLinear)

    ID_problems : [] #List of IDs with known problems

    rt_kind : 'MV_RQ' #Type of RT 

    list_roi : ['Heart'] #List of ROIs to be analysed
   